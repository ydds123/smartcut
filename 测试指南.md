# SmartCut 端到端测试指南（实测版）

## 🎯 测试目标
验证完整的视频处理流程：上传 → 处理 → 实时进度（SSE）→ 结果查看。

## 📌 当前基线（更新于 2026-02-19）
- 前端地址：`http://localhost:5173`
- 后端地址：`http://127.0.0.1:8000`
- Redis：`127.0.0.1:6379`
- 测试视频（本轮指定）：
  `/Users/apple/Documents/Claude Code/01_项目/短片拉片工具/smartcut/backend/data/uploads/ede532b0-8d18-4e9d-a192-9651b9597250_黑色环保动画短片《转折点》，当人类成为濒危动物时，世界会怎样？.mp4`

## 📋 测试前检查

### 1. 启动服务栈（推荐）
```bash
# macOS
./tools/devstack.sh up

# Windows (PowerShell)
powershell -ExecutionPolicy Bypass -File .\tools\devstack.ps1 up

# 或通用命令
python tools/devstack.py up
```

### 2. 服务检查
```bash
# devstack 状态
python tools/devstack.py status
python tools/devstack.py ps

# 单服务重启（示例：仅重启 backend）
python tools/devstack.py restart backend

# Backend
curl --max-time 5 http://127.0.0.1:8000/
curl --max-time 5 http://127.0.0.1:8000/health

# Frontend
curl -I http://localhost:5173/

# Redis
redis-cli -p 6379 ping

# Worker（兼容两种进程形态）
ps -ef | rg "app.workers.worker|rq worker"
```

### 3. 测试视频检查
```bash
VIDEO="/Users/apple/Documents/Claude Code/01_项目/短片拉片工具/smartcut/backend/data/uploads/ede532b0-8d18-4e9d-a192-9651b9597250_黑色环保动画短片《转折点》，当人类成为濒危动物时，世界会怎样？.mp4"
ls -lh "$VIDEO"
```

---

## 🚀 标准测试步骤

### Step 1: 打开前端
访问：`http://localhost:5173`

预期：
- SmartCut 标题可见
- 上传区域可见
- 任务列表可见

### Step 2: 上传指定视频
1. 点击上传区域或拖拽文件
2. 选择上述 `VIDEO` 路径
3. 等待上传完成

预期：
- 新任务卡出现
- 标题显示为上传文件名
- 初始状态为 `PENDING`

### Step 3: 开始处理
在新任务卡点击 `开始处理`

预期：
- 状态 `PENDING -> QUEUED -> PROCESSING`
- 开始出现进度条

### Step 4: 观察实时进度
持续观察任务卡和网络请求。

预期：
- 进度单调递增，最终到 `100%`
- 状态最终为 `COMPLETED`
- `查看结果` 按钮出现

### Step 5: 查看结果
点击 `查看结果`

预期：
- 时间轴弹窗出现
- 场景缩略图可见
- 场景数量和时间信息可见

---

## 🔍 任务级验收（推荐）

不要用“库里只有 1 条任务”作为判断标准，应按“本次新增 task_id”验收。

### Checkpoint A: 新任务识别
```bash
# 测试前
curl --max-time 8 -sS http://127.0.0.1:8000/api/tasks > /tmp/tasks_before.json

# 测试后
curl --max-time 8 -sS http://127.0.0.1:8000/api/tasks > /tmp/tasks_after.json

# 找到新增 task_id
jq -s '.[0] as $b | .[1] as $a | ($a|map(.id) - ($b|map(.id)))' \
  /tmp/tasks_before.json /tmp/tasks_after.json
```

### Checkpoint B: 状态与进度
```bash
TASK_ID="<新增 task_id>"
curl --max-time 8 -sS "http://127.0.0.1:8000/api/tasks/$TASK_ID"
```

预期：
- `status = COMPLETED`
- `progress = 100`
- `total_scenes` 有值（本次实测视频通常为 57）

### Checkpoint C: 数据库记录
```bash
cd "/Users/apple/Documents/Claude Code/01_项目/短片拉片工具/smartcut/backend"
sqlite3 ./data/database.db \
  "SELECT id, display_name, status, progress, total_scenes FROM tasks ORDER BY created_at DESC LIMIT 5;"
```

### Checkpoint D: 任务文件
```bash
TASK_ID="<新增 task_id>"
ls -lh "./data/tasks/$TASK_ID/scenes/" | head
```

---

## 🐛 常见问题排查（已结合 2026-02-19 实际问题）

### 问题 1: `/api/tasks` 或 `/progress` 返回 500
症状：
- 前端报 `Network Error`
- 后端日志出现 `QueuePool limit ... TimeoutError`

排查：
```bash
tail -n 200 /tmp/smartcut-backend.log
```

处理建议：
1. 确认仅对 `QUEUED/PROCESSING` 任务建立 SSE（不要对 `PENDING` 建连）
2. SSE 查询使用短会话，避免长期占用连接池
3. 重启 backend + worker 后重测

### 问题 2: 进度停在某个值（如 45%）
症状：
- 后端任务已完成，但前端卡在中间进度

排查：
```bash
curl --max-time 8 -sS "http://127.0.0.1:8000/api/tasks/<TASK_ID>"
```

如果后端已 `COMPLETED/100` 而前端未刷新，优先检查：
1. SSE 是否 500 或断连
2. 前端是否对完成状态做了兜底同步

### 问题 3: 端口或协议不一致
症状：
- 浏览器能开页面，但脚本/接口连接拒绝

处理：
1. 前端用 `localhost:5173`
2. 后端 API 用 `127.0.0.1:8000`（不要写 `localhost:8000`，避免解析到 `::1`）
3. 不要混用旧端口 `5174/8787`

---

## 📊 成功标准
- [ ] 视频上传成功并生成新任务
- [ ] 任务状态正确流转：`PENDING -> QUEUED -> PROCESSING -> COMPLETED`
- [ ] 前端进度条单调递增并到达 `100%`
- [ ] SSE 请求稳定（无持续 500）
- [ ] 结果页可打开且场景/缩略图正常
- [ ] 后端 `task` 记录与前端状态一致

---

## 🧪 最近一次实测结论（2026-02-19）
- 指定视频无头测试通过
- 指定视频可见浏览器测试通过
- 两轮新增任务最终均为 `COMPLETED`，`progress=100`，`total_scenes=57`
