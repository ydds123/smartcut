# 01 产品定义

## 一句话定义

这是一个面向动画短片、微电影等叙事型视频的镜头边界确认工作台。

它的核心价值不是“全自动给出完美分镜”，而是把人工从头拉片校对，变成对算法候选结果进行快速确认和修正。

## 问题定义

当前视频场景切分存在两个核心问题：

- 碎切：同一镜头内部因为光变、闪白、剧烈运动、特效等被误切成多个边界。
- 漏切：真实镜头切换，尤其是 dissolve、fade、wipe 等渐变过渡没有被识别出来。

对叙事视频来说，单纯的像素变化检测并不等于可用的镜头边界结果。最终往往仍需要人工核对，判断切分是否符合叙事维度上的镜头组织逻辑。

## 目标视频与目标用户

目标视频：

- 动画短片
- 微电影
- 其他强调镜头语言和转场设计的叙事型视频

目标用户：

- 需要做镜头切分、拉片、预剪或片段管理的人
- 需要将切分结果导出到下游剪辑、分析或自动化流水线的人

## 产品定位

本项目当前不定位为“全自动叙事分镜器”，而定位为：

- 两阶段镜头边界检测引擎
- 带人工校验与修正的 review harness
- 可输出标准格式结果的可复用能力层

## V1 最小闭环

V1 只聚焦一个最小闭环：

```text
本地视频输入
→ 两阶段切分
→ 分镜预览
→ 人工修正
→ 标准导出
```

其中“分镜预览”不是附属 UI，而是算法到可用结果之间的 Harness。

## V1 成功标准

V1 的判断标准不是模型分数最高，而是：

- 对单个 20 分钟左右的视频可以跑通完整流程
- 输出的边界结果可视、可校验、可修正
- 人工核对时间明显少于从头手工拉片
- 最终结果可导出给下游工具使用

## V1 明确不做

以下内容不是 V1 重点：

- 云端抓取、下载与分发
- 插件平台化
- 节点式工作流系统
- 大规模文件管理平台
- 自动叙事段落划分
- 全自动替代人工确认

## 关键判断

当前阶段最重要的产品判断有两条：

- 先证明“人工确认成本显著下降”
- 先打穿“镜头边界确认工作台”这个窄但强的 wedge

## 为什么先做最小闭环而不是大平台

当前阶段最关键的未知数不是云端抓取、插件、SDK 或平台分发，而是：

- 两阶段切分结果是否真的能显著降低人工校对成本
- review harness 是否足够把算法候选转化为可确认结果

因此，工程和产品都应该先围绕“最小闭环”做验证，而不是提前建设外围平台能力。
