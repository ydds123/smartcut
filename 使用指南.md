# SmartCut 使用指南

> 快速上手 · 部署后使用

---

## 🚀 启动服务

### 一键启动（推荐）

在项目目录执行：

```bash
cd smartcut
./tools/devstack.sh up
```

启动后会自动运行：
- 前端：`http://localhost:5173`
- 后端：`http://127.0.0.1:8000`
- Redis + Worker

### 常用运维命令

```bash
# 查看状态
./tools/devstack.sh status

# 重启单个服务
./tools/devstack.sh restart backend
./tools/devstack.sh restart frontend

# 查看日志
./tools/devstack.sh logs backend --tail 50

# 停止所有服务
./tools/devstack.sh down
```

---

## 📖 使用步骤

### 1. 打开前端

浏览器访问：`http://localhost:5173`

### 2. 上传视频

- **支持格式**：MP4, MOV
- **大小限制**：500MB
- **操作**：拖拽文件到上传区，或点击选择文件

### 3. 开始处理

上传完成后，任务卡片会自动出现。点击 **"开始处理"** 按钮。

### 4. 等待完成

- 进度条会实时显示处理进度（0% → 100%）
- 处理完成后会显示 **"已完成"** 状态

### 5. 查看结果

点击 **"查看结果"** 按钮，可以看到：
- 每个场景的时间码（起止时间）
- 场景缩略图
- 片段预览
- 批量下载所有片段

---

## 🎯 适用场景

| 用户 | 场景 |
|------|------|
| **视频剪辑师** | 从长素材中快速提取有效镜头 |
| **内容运营** | 分析竞品视频的镜头节奏和结构 |
| **普通用户** | 把旅游长视频切开发朋友圈 |

---

## ❓ 常见问题

### Q: 前端打不开？
A: 检查前端服务是否启动：`./tools/devstack.sh status`

### Q: 上传后没反应？
A: 检查后端服务：`curl http://127.0.0.1:8000/health`

### Q: 进度卡住不动？
A: 查看后端日志：`./tools/devstack.sh logs backend --tail 50`

### Q: 如何删除任务？
A: 任务卡片右上角有删除按钮

---

## 📂 文件说明

处理后文件位置：
```
smartcut/data/tasks/{任务ID}/
├── original.mp4           # 原始视频
└── scenes/
    ├── 001_000000_000015.mp4
    ├── 002_000015_000045.mp4
    └── ...
```

---

**最后更新**：2026-02-22
